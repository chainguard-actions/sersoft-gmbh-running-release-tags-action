name: Test non-semver tag with fail-on-non-semver-tag=true (dry-run)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      ACTIONS_STEP_DEBUG: 'true'
      RUNNER_DEBUG: '1'
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          tag: not-a-semver-tag
          dry-run: 'true'
          create-release: 'false'
          fail-on-non-semver-tag: 'true'
      - name: Assert action failed as expected
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail for non-semver tag with fail-on-non-semver-tag=true, but got: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed for non-semver tag"
